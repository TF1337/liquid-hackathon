# Experimental Schemas

This directory is reserved for optional, vertical-specific extraction schemas.

- Do not treat files here as product defaults.
- Keep `schemas/base/minimal_evidence.schema.json` as the neutral default until the team locks a vertical.
- Experimental schemas must stay Stage 1 extraction-only (visible evidence only, no recommendations or business reasoning).
